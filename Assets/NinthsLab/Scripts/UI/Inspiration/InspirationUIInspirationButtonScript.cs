using Nova;
using UnityEngine;

public class InspirationUIInspirationButtonScript : MonoBehaviour
{
    public InspirationDataType inspirationData;
    public InspirationDataType parentInspirationData;
    public Vector2 SideLocation;
    public bool isCentered;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
